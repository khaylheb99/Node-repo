const mode = require('./My_modules');

const hey = require('./Intro');

const Info = require('./alternatives');

console.log(Info);

// If the function in a module isn't assigned 
// to a variable, it will run automatically 
// when we use require

require('./Summation');

//Built-in modules
//->os
//->path
//->fs
//->http